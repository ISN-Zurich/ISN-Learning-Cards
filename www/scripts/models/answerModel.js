function AnswerlModel() {
	this.answerList = [];
	this.index = 0;
	
};

AnswerModel.prototype.getUserAnswer = function() {};

AnswerModel.prototype.setUserAnswer = function() {};

AnswerModel.prototype.getAnswerResults = function() {
	
	// to return the the 3 different answer results
	// excellent
	//partially correct
	//wrong
};

//to define a getId() method that will return back the answer id. this id would be a number

//the different possible answers of each question will be stored in the local storage, but
//the "taped" answers of the learner will not be stored. they will be passed on a variable